/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EkoDataTableColumnDirective } from './eko-data-table-column.directive';

describe('Directive: EkoDataTableColumn', () => {
  it('should create an instance', () => {
    const directive = new EkoDataTableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
